# BATCH-126 — Canon Candidates

## Status Note
These are candidate canonical fragments extracted from a derived summary source.
They are not promoted to canon by inclusion here.

## Candidate Fragments
1. Everything is a block; some blocks are larger because they are compressed assemblies of other blocks.
2. MOLT decides who is allowed to win; priority decides which one wins when the same kind collide.
3. Bundle uses priority to order influence; merge uses priority to resolve conflict.
4. Merge is a terminal operation that combines resolved outputs into a single RuntimeSpec; it never operates on raw blocks or alters authority.
5. Cross-MOLT merging is always allowed as structured assembly into RuntimeSpec buckets; cross-MOLT synthesis is allowed only through explicit typed operators; untyped blob fusion is never allowed.
6. Block attachment defines scope of application, not authority.
7. The UMG Compiler takes modular cognition and deterministically resolves it into a clean, auditable RuntimeSpec, along with a full forensic Trace of how that result was produced.
8. NeoBlock = compressed Column.
9. NeoStack means these NeoBlocks belong together.

## Promotion Caution
Before canon lock, compare these fragments against:
prior evidence,
existing compiler behavior,
current documentation wording,
and competing formulations about hierarchy vs governance.
