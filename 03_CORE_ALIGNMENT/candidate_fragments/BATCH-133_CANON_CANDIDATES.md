# BATCH-133 — Canon Candidates

## Status Note
These are candidate canonical fragments extracted from a derived integration summary with implementation exposure.
Inclusion here does not itself promote them to canon.

## Candidate Fragments
1. UMG stays the deterministic brain-spec; LangChain becomes the execution harness.
2. UMG compiles → LangChain runs.
3. Runtime and Trace are separate artifacts.
4. Document the compiler pipeline as reflected in `compile.ts`.
5. Document bundle semantics as ranked versus alternates.
6. Document tested priority resolution rules rather than only abstract priority language.
7. Prompt/render text should be derived from runtime structure afterward, not conflated with runtime structure itself.
8. Runtime indexes are UI/search helpers distinct from semantic runtime selection.

## Promotion Caution
Before canon lock, compare these fragments against:
broader glossary and compiler batches,
current code realities,
existing documentation wording,
and later control-room decisions about adapter-layer behavior.
