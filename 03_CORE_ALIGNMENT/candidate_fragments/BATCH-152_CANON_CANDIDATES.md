# BATCH-152 — Canon Candidates

## Status Note
These are candidate canonical fragments extracted from a derived runtime-bridge summary.
Inclusion here does not itself promote them to canon.

## Candidate Fragments
1. The implementation surface is not the agent’s native environment; it is a controlled action surface.
2. The proxy terminal acts as the agent’s hands; the browser IDE acts as the visible workspace.
3. Plan elsewhere, act through bounded primitives, reflect results visibly.
4. Require workspace scoping, audit logging, and rollback for agent-facing bridges.
5. Distinguish cloud-preview runtime from local/prod runtime.

## Promotion Caution
Before canon lock, compare these fragments against:
existing runtime-boundary language,
later control-room decisions on bridge APIs,
and any formal agent execution contract work.
