# BATCH-182 - Canon Candidates

## Status Note
These are candidate canonical fragments extracted from a derived Builder implementation summary.
Inclusion here does not itself promote them to canon.

## Candidate Fragments
1. UMG is the framework; UMG Builder is one JSON-based visual tool built on top of it.
2. Builder uses static JSON only; no backend or database is required for the current app.
3. Manifest-driven plan selection auto-populates the Builder stack.
4. Runtime stack behavior uses both per-block logic and matrix files.
5. Keep Builder manifest documentation separate from framework-wide UMG definition.

## Promotion Caution
Before canon lock, compare these fragments against:
- framework-wide UMG docs
- Builder implementation docs
- later compiler/runtime batches
- any finalized decision on Meta/runtime overlay categories
