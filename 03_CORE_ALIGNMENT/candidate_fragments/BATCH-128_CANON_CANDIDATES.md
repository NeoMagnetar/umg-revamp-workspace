# BATCH-128 — Canon Candidates

## Status Note
These are candidate canonical fragments extracted from a derived summary artifact.
Inclusion here does not itself promote them to canon.

## Candidate Fragments
1. Priority does not change MOLT hierarchy.
2. Governance is the only override mechanism.
3. JSON is a declarative specification, not imperative code.
4. It is a blueprint, not a script.
5. Bundle and merge selected blocks inside a stack are not the same thing as compressing a whole stack into a NeoBlock.
6. Sleeve JSON is the authored source of truth.
7. UI visualization state is not compiler truth.
8. The compiler can be published independently of the UI.
9. RuntimePromptSpec should describe what the model would actually see.

## Promotion Caution
Before canon lock, compare these fragments against:
prior compiler glossary and release batches,
current compiler behavior,
existing documentation wording,
and any later control-room decisions on output artifacts.
