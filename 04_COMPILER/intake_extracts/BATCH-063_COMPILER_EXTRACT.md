# BATCH-063 Compiler Extract

## Direct compiler/runtime signals
- Parse active blocks or typed MOLT units.
- Prepare a composite prompt or plan.
- Treat **Primary** as backbone or main directive.
- Inject **Merge** content as additive facts, data, constraints, or requirements.
- Apply **Blueprint** as style / structural / worldview influence rather than literal content insertion.
- Support optional multi-step generation instead of one-pass concatenation only.
- Preserve iterative regeneration by editing isolated blocks.

## PoeUMG/runtime additions
- Build a context map before rendering.
- Use MOLT-driven response rendering.
- Inject reflection as part of runtime.
- Support recursive loop:
  - Understand
  - Reflect
  - Refine

## Self-adaptive additions
- Pre-response assembly from active MOLTs.
- Post-response adjustment based on feedback.
- Support decay, reinforcement, mutation, or reweighting of remembered MOLTs.

## Likely spec questions
1. Is Blueprint compile-time influence, runtime filter, or both?
2. What is the precedence order between typed MOLT units and user-facing block roles?
3. What exactly is the merge precedence model?
4. Are reflection and post-response adaptation required runtime phases or optional extensions?
5. How should mutable MOLTs be audited, traced, and rolled back?

## Stage 1 compiler implication
This batch supports building a reconciliation spec before implementation changes. The main need is not a patch yet, but a normalized compiler/runtime model that all document layers can point to.
