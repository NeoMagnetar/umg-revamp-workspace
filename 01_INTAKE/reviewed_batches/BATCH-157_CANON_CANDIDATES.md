# BATCH-157 — Canon Candidates

## Candidate Canon Language
- UMG is an orchestration architecture, not a single model.
- UMG is a modular thought architecture and systems wrapper for composing models, tools, retrieval, and solvers.
- UMG can wrap latent specialist engines while preserving explicit, auditable control flows.
- Specialist solvers may be invoked selectively under UMG routing, verification, and fallback governance.

## Candidate Naming
- `hrm.solver` as a provisional specialist-solver wrapper name

## Candidate Documentation Moves
- explain UMG as the wrapper layer around external specialist reasoners
- contrast latent internal reasoning engines with explicit UMG audit surfaces
- document planning / execution / verification as distinct layers in wrapped-solver workflows

## Candidate PRD / Staging Moves
- GPU readiness → local solver install → wrapper integration → evaluation → offline submission packaging

## Canon Risk Note
These are candidates only. The batch contains strong language, but naming and abstraction boundaries remain unresolved.
