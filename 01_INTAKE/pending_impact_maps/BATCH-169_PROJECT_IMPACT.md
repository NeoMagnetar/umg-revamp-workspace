# BATCH-169 — Project Impact Map

## Primary Impact
This batch materially impacts the UMG revamp because it pushes toward a **canonical block system** where semantics, composition rules, and runtime behavior are encoded inside the block itself. That has direct implications for semantic stabilization, compiler/runtime design, documentation, and the staged builder product. fileciteturn19file0

## Impact by Project Area

### 1. Core Alignment
- Canonical block role set tightened.
- Merge elevated from general mechanism into candidate/operator block territory.
- Snap/stack/merge treated as encoded block behavior instead of vague conceptual verbs.

### 2. Compiler / Runtime
- Schema-key normalization required.
- Sleeve-loading logic needs one authoritative path.
- Registry repair and runtime dispatch alignment become first-order tasks.
- Runtime flags and optional module hooks become part of block interpretation.

### 3. Documentation
- Schema guide becomes more than a field list; it becomes an ontology and authoring contract.
- Copyable JSON examples become necessary documentation artifacts.
- Color grammar and directory conventions need consistency.

### 4. Skill / Builder Workflow
- Builder should support manual creation, AI-assisted fill, import/export, snap-aware composition, and visible JSON inspection.
- Command repair, sleeve load, and block maintenance should become stable callable workflows.

### 5. PRD / Staging
- Supports an import/export-first Bolt.new builder MVP.
- Optional Poe/OpenAI assistance should remain toggleable.
- Suggests phased rollout before full block-library scale. fileciteturn19file0

## Immediate Follow-On Questions for Later Synthesis
- Is Merge formally canonical, or a system/operator block?
- Which field names survive: `canto_overlay` and `merge_behavior`, or earlier variants?
- What is the minimum canonical block schema?
- What is the exact authoritative runtime path for sleeve activation and state copy?
