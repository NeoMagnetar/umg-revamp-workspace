# BATCH-071 — Blocks / MOLT Extract

## Why This Optional Extract Exists
This batch explicitly proposes role distinctions, stack ordering, and control semantics around Alignment. That is enough to justify a focused taxonomy extract even though the source is still derivative.

## Role Signals Extracted
- **Primary** -> defines what to do
- **Subject** -> defines the domain or topic
- **Instruction** -> defines how to do it
- **Philosophy** -> defines the lens or worldview
- **Alignment** -> proposed as what governs whether action is acceptable / allowed

## Taxonomy Implication
The source does **not** cleanly establish whether Alignment is:
1. a new MOLT role,
2. a meta-role that governs all other roles,
3. or a separate governance system attached to stacks.

That ambiguity must be preserved.

## Stack / Hierarchy Signals
The source implies a stack ordering or visible hierarchy roughly like:
- Primary
- Subject
- Philosophy
- Instruction
- Alignment
- Trigger
- Vault Header

This ordering should be treated as conceptual evidence only, not canonical ordering.

## Behavior Signals
- Alignment influences whether execution proceeds.
- Merge must preserve coherence across stacked blocks.
- Blueprint remains separate from core content-generation logic.
- Alignment is treated as validity/governance, not merely style or topic.

## Immediate Use
- Feed into taxonomy synthesis
- Compare against existing Philosophy, Trigger, and governance semantics
- Use for documentation clarification around block-role boundaries
