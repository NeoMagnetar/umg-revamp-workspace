# BATCH-140 — Sleeve Extract

## Scope
Sleeve-like identity packaging and resleeving signals extracted from the source.

## Persona-as-Sleeve Signal
The Wooden persona is treated implicitly as a sleeve-like identity package.
Its core form is decomposed into modular units rather than monolithic prose.

## Resleeving Pattern
The source implies that swapping JSON config and identity blocks allows the runtime to produce different persona systems.
This suggests a resleeving pattern through configuration rather than deep code rewrite.

## Stable Base / Swappable Identity Pattern
- base runtime logic remains relatively stable
- persona identity and behavior blocks are externalized into config
- new bots can be generated from new structured identity definitions

## Sleeve-Level Doctrine
- separate static identity from dynamic behavior logic
- keep runtime code reusable
- inject or mount specialized identity packages through config
- package the resulting system as a deployable unit
